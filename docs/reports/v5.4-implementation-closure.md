# v5.4 Implementation Closure Report

Date: 2026-06-11

## Summary

v5.4 的冻结计划 `docs/v5.4-code-plan.md` 已按代码级目标完成一轮实现闭合。本轮没有修改冻结计划本身；新增和修改集中在 daemon/MCP 控制面、hook policy、HTTP 本地边界、路径边界、以及测试。

## Closed Items

| Area | Result | Evidence |
| --- | --- | --- |
| MCP/session compact views | `summary/tui/events/debug/raw` 分层，legacy include 不能把 raw 塞回 compact surface | `mcp::tests::default_session_reads_projection_without_live_pty`, `session_events_are_compact_unless_raw_view_requested`, `tui_view_ignores_legacy_raw_includes` |
| Response hard budget | compact `summary/tui/events` 会裁剪数组/长字符串并附带 `budget`，超限时返回小型 notice | `mcp::tests::compact_events_view_enforces_hard_budget`, `tui_view_enforces_hard_budget_when_route_paths_are_noisy` |
| Route short transaction | route start 不再等待 hook ack，不重复投递 prompt；ack 缺失投影为 `prompt_ack_missing` | `routes::tests::route_prompt_gate_dispatches_once_without_waiting_for_hook_ack`, `mcp::tests::tui_view_projects_prompt_ack_missing_after_deadline` |
| Actor / command priority | stop/kill/interrupt 优先于普通输入和 raw write；actor panic 写失败投影 | `actor::tests::actor_backlog_pops_control_commands_before_normal_input_and_raw_write`, `actor_panic_guard_projects_actor_failed` |
| Stop vs kill lifecycle | stop/kill 拆分，lease 释放保留到 waiter 确认退出后 | `session::tests::stop_and_kill_lifecycle_requests_keep_lease_release_in_waiter` |
| Destructive preconditions | stop/kill/interrupt/plan transition 需要 projection seq + owner lease + generation | `commands::tests::destructive_command_requires_current_owner_lease_precondition`, `destructive_command_rejects_owner_mismatch`, `ownership::*lease*` tests |
| Binding trust | route worker 写操作和非只读 Bash 在 binding missing/untrusted 时 fail closed | `hooks::tests::pty_route_direct_policy_denies_untrusted_wrapper_binding`, `active_route_denies_unbound_write_until_hook_env_binds` |
| Path diagnosis | policy denial blocker 包含 process cwd、target workspace、scratch root、matched root、expected/actual decision | `hooks::tests::policy_denial_records_path_diagnosis_with_materialized_roots` |
| HTTP local boundary | `/api/*` 需要 token，拒绝非 loopback Host/Origin，去掉 `Access-Control-Allow-Origin: *` | `http::tests::api_requires_token_when_loopback_dev_mode_is_disabled`, `api_rejects_non_loopback_origin_even_with_token` |
| Path traversal | context id 必须是 safe segment；transcript index 不能逃出 workspace/configured roots | `routes::tests::context_rejects_path_traversal_ids`, `transcript_index_rejects_path_outside_allowed_roots` |
| MCP input cap | MCP stdio 超 1 MiB 行被拒且 transport 保持可用 | `protocol::tests::bounded_reader_rejects_oversized_line_and_keeps_next_request` |

## Validation

`python agentcall.py release-check` passed:

- Python compileall: passed
- supervisor skill check: passed
- architecture audit: passed
- node syntax check: passed
- plugin validation: passed
- cargo workspace tests: passed
  - `agentcall-daemon`: 136 passed
  - `agentcall-hook`: 2 passed
  - `agentcall-mcp`: 8 passed
- pytest: 17 passed
- git diff whitespace check: passed

## Local Runtime Note

The daemon now enforces token auth for `/api/*` unless `dev_open_loopback=true` is set. The untracked local config `config/agentcall.local.json` has been updated locally with a daemon token so MCP and hook scripts can continue to call the daemon after restart. The committed template only contains `daemon_token: null`.
